import React from "react";
import ReactDOM from "react-dom";

const Options = () => {
  return (
    <>
      <h1>Options</h1>
    </>
  );
};

ReactDOM.render(
  <React.StrictMode>
    <Options />
  </React.StrictMode>,
  document.getElementById("root")
);
